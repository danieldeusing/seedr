{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The main window: core IPC plus window dragging — the overlay title bar is markup, so data-tauri-drag-region needs start-dragging, which core:default does not include. The dialog and the opener are driven from Rust and need no grant.",
  "windows": ["main"],
  "permissions": ["core:default", "core:window:allow-start-dragging"]
}
