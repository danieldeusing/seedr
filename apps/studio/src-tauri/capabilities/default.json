{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The main window: core IPC only — the dialog and the opener are driven from Rust, which needs no capability grant, so the webview gets none",
  "windows": ["main"],
  "permissions": ["core:default"]
}
