{
  "name": "@seedr/registry-ops",
  "version": "0.1.0",
  "private": true,
  "description": "Deterministic registry operations: paths, the one validator, the label catalogue, reads, add/update/remove/set-labels, compile, transactions. No UI, no agent, no shell. Exported as source on purpose — pnpm compile and the agent hook must work on a fresh clone before anything is built.",
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts"
    },
    "./pure": {
      "types": "./src/pure.ts",
      "import": "./src/pure.ts"
    }
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint src/",
    "lint:fix": "eslint src/ --fix",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@seedr/shared": "workspace:*",
    "@types/node": "^25.0.0",
    "typescript": "^5.7.0",
    "vitest": "^4.0.0"
  }
}
