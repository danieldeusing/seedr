name: CI

on:
  push:
    branches: [main]
  pull_request:

permissions: {}

jobs:
  validate:
    uses: ./.github/workflows/validate.yml
    permissions:
      contents: read
    with:
      ref: ${{ github.sha }}
